name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build:gh
        env:
          VITE_SUPABASE_URL: https://jxmfhrqouhirqshbkljk.supabase.co
          VITE_SUPABASE_PUBLISHABLE_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imp4bWZocnFvdWhpcnFzaGJrbGprIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njg2NTQ4ODgsImV4cCI6MjA4NDIzMDg4OH0.OyJ_jF52ong4c5G8VbIPBFdniu8JyE3Id8THF1I8WxM
          VITE_SUPABASE_PROJECT_ID: jxmfhrqouhirqshbkljk

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          cname: # Optional: add your custom domain here
